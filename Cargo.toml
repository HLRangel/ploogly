[package]
name = "ploogly"
version = "0.5.0"
edition = "2021"

[dependencies]
actix-web = "4"
actix-files = "0.6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
markdown = "1.0.0"

[features]
quiet = []
